// verify: debug error:E0425
// Listing 17.4-5: rustc's resolver reports an unknown name with a spelling suggestion.
fn main() {
    let total = 10;
    let doubled = totl * 2;
    println!("{doubled}");
}
